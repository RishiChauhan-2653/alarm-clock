# python alarm clock
a simple alarm clock built using Python and Pygame.
## Features
- set an alarm time
- plays an mp3 when the alarm rings
- uses datetime and pygame
# Requirements
- Python 3
- Pygame

  install pygame: pip install pygame
  run: python alarm.py
